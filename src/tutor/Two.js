import { motion } from "framer-motion";
import "../Boxes.css";

const Two = () => {
  return (
    <div>
      <motion.div animate={{ rotate: "180deg" }} className="boxes">
        khjS
      </motion.div>
    </div>
  );
};

export default Two;
